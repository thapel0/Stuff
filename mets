Scenario:

Given the following starter code and data.csv file, complete the function so that it returns the mean daily return for July 2021. Your function should return a single value rounded to five decimal points.

Starter Code:

import pandas as pd
from data import data 

def calcMean():
    df = pd.DataFrame(data)
    df.set_index(
        pd.to_datetime(
            df['date'],
            infer_datetime_format=True), 
        inplace=True
    )
